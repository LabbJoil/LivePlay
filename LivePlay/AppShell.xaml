<?xml version="1.0" encoding="UTF-8" ?>
<Shell
    x:Class="LivePlay.AppShell"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:enterPage="clr-namespace:LivePlay.Pages"
    Shell.FlyoutBehavior="Flyout"
    Title="LivePlay">
    <!-- TODO: сделать загрузочный экран  вместо enterPage-->
    <Shell.Resources>
        <ResourceDictionary>
            <Color x:Key="FlyoutBackgroundColor">LightBlue</Color>
            <!-- Цвет, который хотите установить для Flyout -->
            <!--<Style TargetType="FlyoutItem">
                <Setter Property=""  Value="{StaticResource FlyoutBackgroundColor}" />
            </Style>-->
        </ResourceDictionary>
    </Shell.Resources>
    
    <ShellContent
        Title="Home"
        ContentTemplate="{DataTemplate enterPage:MainPage}"
        Route="MainPage" />
</Shell>
